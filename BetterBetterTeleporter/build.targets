<Project>
  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Message Text="Copying $(TargetPath) to dev profile..." Importance="high" />
    <Copy
      SourceFiles="$(TargetPath)"
      DestinationFolder="$(DevOutDir)"
      OverwriteReadOnlyFiles="true" />
  </Target>
</Project>